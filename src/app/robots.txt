import { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: ['/api/', '/admin/'], // যে পেজগুলো ইনডেক্স করাতে চান না
    },
    sitemap: 'https://usanewsflow.com/sitemap.xml',
  }
}